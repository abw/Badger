To release a new version:

* Update VERSION in Makefile.PL and $VERSION in lib/Badger.pm

* Update the pod/Badger/Changes.pod file and regenerate Changes:

  $ pod2text pod/Badger/Changes.pod > Changes

* Build:

  $ perl Makefile.PL
  $ make
  $ make test
  $ make dist

